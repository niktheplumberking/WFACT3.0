{
  "name": "@wfact/frontend-loop",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 4 stand-in for the Motion Sites + 21st.dev front-end loop — see README.md for the fallbacks taken and CLAUDE.md §8 for the substitution-disclosure rule this follows.",
  "type": "module",
  "scripts": {
    "build-page": "tsx src/cli.ts",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
