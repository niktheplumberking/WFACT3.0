# WFACT 3.0 — environment variable shape, never real values.
# Per CLAUDE.md §6: secrets live in a secrets manager, never plaintext env vars long-term.
# This file exists so a real .env can be assembled quickly once credentials land
# (see BLOCKED-ON-NICK.md) — copy to .env.local, fill in, never commit .env.local.

# --- State layer (Phase 2) ---
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Controller / models (Phase 3-4) ---
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=            # only if/when Codex or GPT-5.6 routing is wired
# KIMI_API_KEY=              # Kimi K3, front-end loop — Phase 4

# --- Front-end MCPs (Phase 4, due Day 9 per BLOCKED-ON-NICK.md) ---
MOTION_SITES_MCP_TOKEN=
TWENTYFIRST_DEV_TOKEN=
HIGGSFIELD_MCP_TOKEN=

# --- Deployment (hosting law: Hostinger live, Vercel preview only) ---
HOSTINGER_API_TOKEN=
VERCEL_TOKEN=

# --- Notifications ---
TELEGRAM_BOT_TOKEN=

# --- Payments (OPEN per Playbook §8 — Bennett & Co account not yet connected) ---
STRIPE_SECRET_KEY=
