# Cockpit — client-side env, VITE_ prefix required for Vite to expose it to the browser bundle.
# Deliberately the ANON key only — see src/supabaseClient.ts's header comment for why.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
